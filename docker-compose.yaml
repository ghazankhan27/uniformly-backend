services:
  db:
    image: postgres:14
    restart: always
    ports:
      - ${PORT}:${PORT}
    environment:
      - POSTGRES_PASSWORD=${PG_PASSWORD}
    volumes:
      - ./data:/var/lib/postgresql/data
